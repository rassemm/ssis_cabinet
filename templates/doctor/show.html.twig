{% extends 'base.html.twig' %}

{% block title %}Doctor{% endblock %}

{% block body %}
    <h1>Doctor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ doctor.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ doctor.FirstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ doctor.LastName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ doctor.Email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ doctor.Phone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ doctor.Adresse }}</td>
            </tr>
            <tr>
                <th>DateOfBirth</th>
                <td>{{ doctor.DateOfBirth ? doctor.DateOfBirth|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_doctor_index') }}">back to list</a>

    <a href="{{ path('app_doctor_edit', {'id': doctor.id}) }}">edit</a>

    {{ include('doctor/_delete_form.html.twig') }}
{% endblock %}
